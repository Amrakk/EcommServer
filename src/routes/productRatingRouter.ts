import express from "express";
import { api } from "../api/index.js";

const productRatingRouter = express.Router();

// productRatingRouter.get("/:id", api.productRating.getAll);

export default productRatingRouter;
